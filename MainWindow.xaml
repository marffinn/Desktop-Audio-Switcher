<Window x:Class="AudioDeviceSwitcher.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Desktop Audio Switcher" Width="400"
        Height="350" Loaded="Window_Loaded" ResizeMode="CanMinimize" Background="White" Icon="/sound-card.png" WindowStyle="None" BorderThickness="1,1,1,1" SnapsToDevicePixels="True" UseLayoutRounding="True">
    <Grid>
        <ListBox x:Name="DeviceListBox"
                 Margin="10"
                 SelectionMode="Single"
                 FontSize="10"
                 SelectionChanged="DeviceListBox_SelectionChanged"
                 VerticalAlignment="Bottom" Background="White">
            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem">
                    <Setter Property="Background" Value="White"/>
                    <Style.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Background" Value="LightGreen"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </ListBox.ItemContainerStyle>
        </ListBox>
        <Rectangle x:Name="MainAppBar" HorizontalAlignment="Center" Height="30" VerticalAlignment="Top" Width="398" Fill="#FF561212" MouseDown="Window_MouseDown"/>
        <Image HorizontalAlignment="Left" Height="16" Margin="217,59,0,0" VerticalAlignment="Top" Width="16" Source="/multiple (1).png"/>
    </Grid>
</Window>
