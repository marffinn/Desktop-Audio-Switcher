<Window x:Class="AudioDeviceSwitcher.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Desktop Audio Switcher" Width="400"
        Height="300" Loaded="Window_Loaded" ResizeMode="CanMinimize" Icon="/sound-card.png" WindowStyle="None" SnapsToDevicePixels="True" UseLayoutRounding="True" Background="#FF462C2C" BorderBrush="#FF0E0E0E" BorderThickness="1,1,1,1">
    <Grid>
        <ListBox x:Name="DeviceListBox" Margin="8,0,8,8" SelectionMode="Single" FontSize="10" SelectionChanged="DeviceListBox_SelectionChanged" Foreground="White" BorderBrush="{x:Null}" Padding="0,0,0,0" BorderThickness="0,0,0,0" Height="2" VerticalAlignment="Bottom" Background="{x:Null}">

            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem">
                    <Setter Property="Background" Value="#FF271D11" />
                    <Setter Property="Foreground" Value="White" />
                    <Setter Property="BorderBrush" Value="{x:Null}" />
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ListBoxItem">
                                <Border Name="Border" Background="{TemplateBinding Background}" Padding="5,2,5,2" SnapsToDevicePixels="True">
                                    <ContentPresenter />
                                </Border>
                                <ControlTemplate.Triggers>
                                    <!-- Hover Trigger -->
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="Border" Property="Background" Value="#FF0B3C19" />
                                    </Trigger>
                                    <!-- Selected Trigger -->
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter TargetName="Border" Property="Background" Value="#FF19756E" />
                                        <Setter Property="Foreground" Value="White"></Setter>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </ListBox.ItemContainerStyle>
        </ListBox>
        <Rectangle x:Name="MainAppBar" HorizontalAlignment="Center" Height="30" VerticalAlignment="Top" Width="400" Fill="#FF231818" MouseDown="Window_MouseDown"/>
        <Button x:Name="Close_btn" HorizontalAlignment="Right" Margin="374,7,8,0" VerticalAlignment="Top" BorderThickness="0,0,0,0" Width="16" Height="16" Click="Close_btn_Click">
            <Button.Background>
                <ImageBrush ImageSource="/icons.png"/>
            </Button.Background>
        </Button>
        <Button x:Name="Mimimize_btn" HorizontalAlignment="Right" Margin="0,7,29,0" VerticalAlignment="Top" BorderThickness="0,0,0,0" BorderBrush="{x:Null}" Width="16" Height="16" Click="Mimimize_btn_Click">
            <Button.Background>
                <ImageBrush ImageSource="/minimize.png"/>
            </Button.Background>
        </Button>
    </Grid>
</Window>
